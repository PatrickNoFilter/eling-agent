"""As Brain — local memory layer MCP server (facts + KB + code + builtin + HRR).

This is the "local brain" for AI agents: all memory layers except Notion.
Use this for fast local storage, retrieval, reasoning, and analysis.
Notion sync is handled separately by the `eling` MCP server.
"""
